import Keyboard from '../Keyboard/Keyboard';
import Inputs from '../Inputs/Inputs';

export default function Game() {
    return (
        <>
            <Inputs></Inputs>
            <Keyboard></Keyboard>
        </>
    );
}
